CustomGame_level1v2

model = DQN("MultiInputPolicy", env, verbose=1, learning_rate=0.0025, batch_size=256,
            gamma=0.99, exploration_initial_eps=1.0, exploration_final_eps=0.025, exploration_fraction=0.95)
model.learn(total_timesteps=2000000, log_interval=20, progress_bar=True)

________________________________________________________________________________________________________________________

CustomGame_level1v3

model = DQN("MultiInputPolicy", env, verbose=1, learning_rate=0.004, batch_size=256,
                        gamma=0.25, exploration_initial_eps=1.0, exploration_final_eps=0.01, exploration_fraction=0.99)
model.learn(total_timesteps=2000000, log_interval=20, progress_bar=True)

________________________________________________________________________________________________________________________

CustomGame_level2v1

model = DQN("MultiInputPolicy", env, verbose=1, learning_rate=0.01, batch_size=256,
                        gamma=0.5, exploration_initial_eps=1.0, exploration_final_eps=0.025, exploration_fraction=0.99)
model.learn(total_timesteps=2000000, log_interval=20, progress_bar=True)

________________________________________________________________________________________________________________________

CustomGame_level2v2

model = DQN("MultiInputPolicy", env, verbose=1, learning_rate=0.001, batch_size=256,
                        gamma=0.99, exploration_initial_eps=1.0, exploration_final_eps=0.05, exploration_fraction=0.99)
model.learn(total_timesteps=2000000, log_interval=20, progress_bar=True)

________________________________________________________________________________________________________________________

CustomGame_level2v3

model = DQN("MultiInputPolicy", env=env, verbose=1, learning_rate=0.1, batch_size=512,
                        gamma=0.25, exploration_initial_eps=1.0, exploration_final_eps=0.1, exploration_fraction=0.99)
model.learn(total_timesteps=2000000, log_interval=20, progress_bar=True)
